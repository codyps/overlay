From 3da9a6c38c832e2d87787f8c1f1c2d627201bcf2 Mon Sep 17 00:00:00 2001
From: Cody P Schafer <jmesmon@gmail.com>
Date: Tue, 5 Mar 2013 16:58:04 -0800
Subject: [PATCH 1/3] change "running from dir" check to look for boot-update
 instead of coreboot.

---
 sbin/boot-update | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/sbin/boot-update b/sbin/boot-update
index 371ffaf..fff755e 100755
--- a/sbin/boot-update
+++ b/sbin/boot-update
@@ -7,7 +7,7 @@ import sys
 
 # This is needed for testing from the root of the git repo - must come before
 # funtoo imports
-if os.path.isdir(".git") and os.path.basename(os.getcwd()) == "coreboot":
+if os.path.isdir(".git") and os.path.basename(os.getcwd()) == "boot-update":
 	sys.path = [ "python/modules" ] + sys.path
 
 # Import Funtoo extensions that we use:
-- 
1.8.1.5

